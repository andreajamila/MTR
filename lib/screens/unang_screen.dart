import 'package:flutter/material.dart';

class UnangScreen extends StatelessWidget {
  const UnangScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
